[project]
name = "rightsiq-mcp-server"
version = "1.0.0"
description = "RightsIQ MCP Server — bridges Databricks Agent Bricks to Salesforce CRM"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "requests>=2.31.0",
]
